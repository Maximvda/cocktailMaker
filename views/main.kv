#:include views/views.kv
#:include views/selector.kv
#:include views/mixer.kv
MDScreen:
    id: main
    # this will create a space navigation bottom
    MDBottomNavigation:
        id: navigation
        # this will create a navigation button on the bottom of screen
        MDBottomNavigationItem:
            name: "selector"
            text: "Create"
            icon: "glass-cocktail"

            Selector:
                id: selector
        MDBottomNavigationItem:
            name: "mixer"
            text: "Mix"
            icon: "tune"

            Mixer:
                id: mixer
